# 观察期（Observation Window）

> **观察期只做一件事：记录信号。不修、不重构、不抽象、不分析。**

## 时间

| | |
|---|---|
| 启动 | **2026-09-19**（V3.1 合并后立即开始） |
| 4 周上限 | **2026-10-17** |
| 状态 | 进行中（窗口开启，尚未达到退出条件） |

## 退出条件（V3.1 M4 冻结）

```text
固定退出：4 周上限（2026-10-17）

提前退出（满足任一）：
  A. ≥3 个相同结构问题（同一 Cluster 至少 3 个信号）
  B. ≥5 个 S1/S2 信号
  C. 出现需要新增 Contract 的候选问题，且已有 ≥2 个独立信号支撑

"C" 的严谨性来自最后半句：单个信号也可能是噪声，必须 ≥2 个独立信号支撑，
防止单点噪声打断冷却期。
```

## 只做什么

- ✅ 把信号写进 `docs/signals/NNNN.md`（照 `TEMPLATE.md`）
- ✅ 记录触发场景 / 期望 / 实际 / 复现 / 严重程度 / 架构影响初判

## 不做什么

- ❌ 不修 / 不重构 / 不抽象 / 不做预防性设计
- ❌ 不写"如果…就…"
- ❌ 不统计分类 / 不做趋势判断 / 不做任何形式的分析
- ❌ 不写 Cluster hint

为什么这么严：**一旦允许"分类"，观察期会退化成分析期；一旦允许"趋势判断"，
分析会退化成设计；一旦允许"设计"，冷却期的意义就消失了。**

## 分类纪律

```text
分类纪律：

  越接近 Kernel 的问题，越不能轻易标 Kernel。
  原因：Kernel 分类会直接触发 V4 Entry 的候选池，
        误标会让 V4 Entry 失去意义。

  升级 Kernel 分类的条件：
    - ≥2 个独立 Provider 的同类信号
    - 该字段需跨 turn 保留
    - 该字段需参与 Contract

  三条不满足时，分类落在最保守的一层。
```

## 禁止隐形设计文档

**观察期会产生"写作冲动"，而写作冲动本身就是设计的雏形。**

观察期内的目录白名单：

```text
允许：
  docs/signals/           ← 信号记录（事实）
  docs/signals/NNNN.md    ← 每个信号一个文件
  docs/clusters/README.md ← 空模板（待冷却期结束才填）

禁止：
  docs/proposals/
  docs/designs/
  docs/rfc/
  docs/architecture-notes/
  docs/ideas/
  docs/v4/*.md（除 ENTRY_CRITERIA.md 和 PROPOSAL_TEMPLATE.md）

例外（允许的"技术文档"）：
  bug reproduction notes（只在 signals/NNNN.md 内，
                          不新建文件）
```

```text
观察期禁止形成隐形设计文档。

禁止：
  - 新建 proposals / designs / rfc / architecture-notes / ideas 目录
  - 在 signals/NNNN.md 之外记录任何"未来应该……"的内容
  - 用 "如果……就……" 形式的记录（这不是事实，是推演）

允许：
  - signals/NNNN.md 内的事实记录
  - 复现命令
  - 环境信息
  - 现象描述
  - Architectural implication 的保守分类

判断标准：
  写下这句话时问自己——这是"发生了什么"还是"应该发生什么"？
    "发生了什么" → 允许
    "应该发生什么" → 禁止
```

**理由**：

```text
观察 ≠ 孵化

一旦允许"把想法写下来"，观察期会退化成：
  写作冲动 → 文档 → 讨论 → 设计 → V4 提前启动

禁止的目的不是禁止思考，
而是禁止把思考具象化。
具象化 = 承诺 = 设计阶段的开始。
```

## 每周只做三件事

1. 数信号数量
2. 数 S1/S2 数量
3. 记录日期

| 日期 | 信号数 | S1/S2 数 | 备注 |
|---|---|---|---|
| 2026-09-19（启动日） | 2（0001、0002） | 0 | 窗口开启当日的真机使用记录 |

## S1 例外

S1（无法完成用户任务）**只在阻塞时**触发记录，不因为"看起来严重"而提前升级处理；
观察期内一律只记录。

## 窗口外的既有发现（归档，**不计入**退出条件）

V2.5 / V3 实施期间已经暴露并已修复的问题不属于本窗口，不参与退出条件计数，
它们留在变更记录里可追溯：

| 发现 | 位置 | 状态 |
|---|---|---|
| 本地端点被系统代理劫持（httpx trust_env 忽略 bypass） | `CHANGELOG_v2_5.md` §「真机验证发现的缺陷」 | 已修 |
| 历史截断产生孤儿 tool 消息（长工具链 400） | `CHANGELOG_v3.md` §十 | 已修 |
| 权限拒绝消耗重试预算 | `CHANGELOG_v3_1.md` 修复 ① | 已修 |
| 当前用户任务被截断类 transform 丢弃 | `CHANGELOG_v3_1.md` 修复 ② | 已修 |

它们**不是**信号：信号必须来自观察期内对 AgentKit 的真实使用。

## 聚类

窗口结束后（时间到或提前退出）才进入聚类，见 `docs/clusters/README.md`。
